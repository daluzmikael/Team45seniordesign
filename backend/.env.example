# Copy this file to `.env` in this folder and set values (never commit `.env`).
#
# POSTGRES_PASSWORD = the password for RDS user VonLindenthal (same one you use when psql prompts).
# Test from a terminal:
#   psql -U VonLindenthal -h nba-sdp-project.cs1c0smw8vqa.us-east-1.rds.amazonaws.com -d postgres
POSTGRES_DB=postgres
POSTGRES_PASSWORD=
OPENAI_API_KEY=
